<Window x:Class="library_management_system.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard"
        Height="1080" 
        Width="1920"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        WindowState="Maximized"
        WindowStyle="None">
        

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <!-- Navigation Bar -->
        <Border Background="#1E1E1E" Height="50" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Library Management System" 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="Bold"
                           VerticalAlignment="Center" 
                           Margin="10,0,0,0" 
                           HorizontalAlignment="Left"/>

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center" 
                            Margin="0,0,10,0">
                    <Button Content="Dashboard" Click="GoToDashboard" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Manage Users" Click="GoToManageUsers" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Borrowing Policies" Click="GoToBorrowingPolicies" Style="{StaticResource NavButtonStyle}" Height="28"/>
                    <Button Content="Book Inventory" Click="GoToBooks" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Reports" Click="GoToReports" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Account" Click="GoToAccount" Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <StackPanel Margin="20,70,20,20">
            <TextBlock FontSize="24" FontWeight="Bold" Foreground="White" Text="Dashboard" Margin="0,0,0,10"/>

            <!-- Library Statistics -->
            <Border Style="{StaticResource PanelStyle}">
                <StackPanel>
                    <TextBlock Text="Library Statistics" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock x:Name="TotalBooksTextBlock" Text="Total Books:" Foreground="White" FontSize="14" Margin="0,5"/>
                    <TextBlock x:Name="ActiveMembersTextBlock" Text="Active Members:" Foreground="White" FontSize="14" Margin="0,5"/>
                    <TextBlock x:Name="OverdueBooksTextBlock" Text="Overdue Books:" Foreground="White" FontSize="14" Margin="0,5"/>
                </StackPanel>
            </Border>

            <!-- Recent Activities -->
            <Border Style="{StaticResource PanelStyle}" Margin="0,10,0,0">
                <StackPanel>
                    <TextBlock Text="Recent Activities" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock x:Name="NewBooksAddedTextBlock" Text="New Books Added:" Foreground="White" FontSize="14" Margin="0,5"/>
                    <TextBlock x:Name="BooksBorrowedTodayTextBlock" Text="Books Borrowed Today:" Foreground="White" FontSize="14" Margin="0,5"/>
                    <TextBlock x:Name="BooksReturnedTodayTextBlock" Text="Books Returned Today:" Foreground="White" FontSize="14" Margin="0,5"/>
                </StackPanel>
            </Border>

            <!-- Quick Actions -->
        </StackPanel>
    </Grid>
</Window>