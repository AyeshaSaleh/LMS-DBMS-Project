<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library_Management_System"
        mc:Ignorable="d"
        x:Class="Library_Management_System.LibrarianMemberRecords"
        Title="L.Member Records" 
        Height="1080" 
        Width="1920" 
        WindowStartupLocation="CenterScreen" 
        WindowState="Maximized" 
        WindowStyle="None"
        Background="#121212">

    <Window.Resources>
        <!-- Sea Green Glow Button Style -->
        <Style x:Key="NavButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#20B2AA" Offset="0.0"/>
                        <GradientStop Color="#008B8B" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#20B2AA" BlurRadius="10" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#00CED1" Offset="0.0"/>
                                <GradientStop Color="#20B2AA" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#00CED1" BlurRadius="15" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="CustomDataGrid" TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#20B2AA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#1E1E1E"/>
            <Setter Property="AlternatingRowBackground" Value="#252525"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="#20B2AA"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderBrush" Value="#008B8B"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="DataGridCellStyle" TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#20B2AA"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#008B8B"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Navigation Bar -->
        <StackPanel Grid.Row="0" Background="#FF1E1E1E" Height="60" VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Library Management System" FontSize="20" FontWeight="Bold" Margin="10,5" Foreground="White"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Dashboard" Style="{StaticResource NavButtonStyle}" Click="Dashboard_Click"/>
                    <Button Content="Book Status" Style="{StaticResource NavButtonStyle}" Click="BookStatus_Click"/>
                    <Button Content="Issue/Reserve" Style="{StaticResource NavButtonStyle}" Click="IssueReserve_Click"/>
                    <Button Content="Member Records" Style="{StaticResource NavButtonStyle}" Click="MemberRecords_Click"/>
                    <Button Content="Fine Collection" Style="{StaticResource NavButtonStyle}" Click="FineCollection_Click"/>
                    <Button Content="Account" Style="{StaticResource NavButtonStyle}" Click="Account_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <!-- Main Content -->
        <Grid Margin="20,80,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Search Members -->
            <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Search Members" FontSize="16" FontWeight="Bold" Margin="10,10,10,5" Foreground="White"/>

                    <StackPanel Orientation="Horizontal" Margin="10,0,10,10">
                        <TextBox x:Name="SearchTextBox" Width="200" Height="35" 
                                 BorderBrush="LightSeaGreen" Background="#121212" Foreground="White"
                                 Padding="5" FontSize="14"/>
                        <Button x:Name="SearchButton" Content="Search" Width="80" Height="35" 
                                Style="{StaticResource NavButtonStyle}" Margin="10,0,0,0"
                                Click="SearchButton_Click"/>
                    </StackPanel>

                    <ListView x:Name="MembersListView" Background="#121212" BorderThickness="0" 
                              Margin="10" Foreground="White" FontSize="14" Height="500"
                              SelectionChanged="MembersListView_SelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" DisplayMemberBinding="{Binding UserId}" Width="50"/>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding FullName}" Width="150"/>
                                <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="200"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Right Panel: Member Details -->
            <Border Grid.Column="2" Background="#1E1E1E" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Member Info -->
                    <StackPanel Grid.Row="0" Margin="10">
                        <TextBlock Text="Member Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="White"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Name:" Width="100" Foreground="White"/>
                            <TextBlock x:Name="MemberNameText" Foreground="White"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="User ID:" Width="100" Foreground="White"/>
                            <TextBlock x:Name="UserIdText" Foreground="White"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Email:" Width="100" Foreground="White"/>
                            <TextBlock x:Name="EmailText" Foreground="White"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Phone:" Width="100" Foreground="White"/>
                            <TextBlock x:Name="PhoneText" Foreground="White"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Borrowing History -->
                    <StackPanel Grid.Row="1" Margin="10,20,10,10">
                        <TextBlock Text="Borrowing History" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="White"/>
                        <DataGrid x:Name="TransactionsDataGrid" Style="{StaticResource CustomDataGrid}"
                                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                                  CellStyle="{StaticResource DataGridCellStyle}" Height="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Book" Binding="{Binding BookTitle}" Width="*"/>
                                <DataGridTextColumn Header="Borrow Date" Binding="{Binding BorrowDate, StringFormat=dd-MM-yyyy}" Width="120"/>
                                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat=dd-MM-yyyy}" Width="120"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>

                    <!-- Update Button -->
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>